#include "../matrix_utils/matrix.h"

void serial_multiply(const Matrix& m1, const Matrix& m2, Matrix& result);
void for_multiply(const Matrix& m1, const Matrix& m2, Matrix& result);
void manual_multiply(const Matrix& m1, const Matrix& m2, Matrix& result);
